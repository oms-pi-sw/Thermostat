#ifndef _UTILS_H_
#define _UTILS_H_

#include <mbed.h>
#include "constants.h"

uint8_t __strlen(const char *s);
float __max(const float x, const float y);
float __round(const float x);

#endif // _UTILS_H_